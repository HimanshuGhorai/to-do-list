const items = document.querySelectorAll('#todo_input');
const list = document.querySelector('#todo_items');

todo_input.addEventListener(
    "keypress",
    function(event){

        console.log(event)
    }
);

